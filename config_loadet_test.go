package go_config_extender

import "testing"

func TestLoadJSONConfigWithEnvOverride(t *testing.T) {
	// 1. Set some techical vars

	// 2. Load config, check envs were applied

	// 3. Drop all techical vars
}
